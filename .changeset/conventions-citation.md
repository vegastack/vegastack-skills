---
"@vegastack/skills": patch
---

Every dev-family skill that ships `references/conventions.md` now cites it from its own SKILL.md, and the register-line format has one home again.

- dev-architect, dev-ship, dev-chronicle, and dev-debug carried the packaged copy with no pointer to it from the agent entry point.
- dev-architect and dev-ship spelled out the register-line format that conventions' Operator identity section owns; both now point at it.
