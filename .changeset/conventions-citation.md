---
"@vegastack/skills": patch
---

Every dev-family skill that ships `references/conventions.md` now cites it from its own SKILL.md, and the register-line format is stated in one place instead of three.

- dev-architect, dev-ship, dev-chronicle, and dev-debug carried the packaged copy with no pointer to it from the agent entry point.
- dev-architect, dev-ship, and dev-setup each spelled out their own variant of the register line; all three now point at conventions' Operator identity section.
- The citations say where the shipped copy sits, so they resolve on a standalone single-skill install.
