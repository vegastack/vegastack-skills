---
"@vegastack/vegafactory": patch
---

`github-app.md`'s hosted-broker reference names the composite action consumers call from a workflow: it ships inside `vegastack/vegafactory` at `packages/broker/action`, referenced by path with the moving `factory-token-v1` tag or a release tag, so there is no separate action repository.
