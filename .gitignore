# Dependencies
node_modules/

# Build outputs (regenerated by prepack/build)
dist/
packages/cli/skill/
packages/cli/skill-integrity.json

# Local packing and scratch output
work/
*.tgz

# Machine-generated evidence artifacts (regenerated in one code path by refresh tooling;
# committing them invites split-brain: a cache and a drift report can disagree at the same timestamp)
.vegastack/evidence-cache.json
.vegastack/evidence-drift.json

# Tool caches and session artifacts
.turbo/
.claude/

# Eval runs — claude plugin eval results and agentskills.io workspaces
**/evals/results/
*-workspace/

# OS noise
.DS_Store
docs/research/
