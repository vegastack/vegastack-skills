{
  "version": 2,
  "_comment": [
    "SkillSpector suppressions for this repo, passed to the scanner with --baseline by",
    "dev-review's scripts/skill-scan.mjs. Discipline lives in skill-maintainer's",
    "references/standards.md: a rule is added only on the operator's word, is scoped as",
    "narrowly as its cause, and its reason carries a 'Still flag if:' clause the guard",
    "enforces.",
    "",
    "SEED ONLY - one rule, for the one cause of issue 61's that a rule can express.",
    "The remaining findings are adjudicated one by one in issue 62; until then the guard",
    "correctly blocks, and widening a rule to reach green is the failure mode, not the fix.",
    "",
    "Issue 62 inherits an open instrument question, with the evidence gathered in 61:",
    "skill-maintainer's four AE1 'reference_unresolved' findings have no precise",
    "suppression here. A rule is too broad - SkillSpector matches on id/path/message",
    "only, and every skill's entry point is named SKILL.md, so {id: AE1, path: SKILL.md}",
    "silences all twelve skills and did hide a regression introduced in branch",
    "feat/skill-scan before it was caught. A fingerprint is too narrow - the generated",
    "baseline collapses the four occurrences into one hash, which on re-scan suppressed",
    "only one of them. Neither is written here rather than pretending the cause is",
    "covered. (A baseline that does carry fingerprints must also set scanner_version.)"
  ],
  "rules": [
    {
      "id": "P2",
      "path": "references/conventions.md",
      "reason": "conventions.md documents the workflow's own comment protocol - the '<!-- vsk:v1 type=... -->' marker every artifact opens with - so a scanner looking for instructions hidden in HTML comments matches the documentation of the mechanism, not an instance of it. The markers are metadata read by guard scripts, carry no imperative, and the file is shared by ten skills, which is why one cause produces ten findings. Still flag if: a marker example gains text addressed to the agent rather than key=value metadata, or the pattern appears in a file that is not documenting the protocol."
    }
  ],
  "fingerprints": []
}
