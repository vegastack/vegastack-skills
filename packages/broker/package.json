{
  "name": "@vegastack/factory-token-broker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that exchanges a GitHub Actions OIDC token for a one-repository VegaStack Factory installation token",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:preview": "wrangler deploy --env preview",
    "deploy:production": "wrangler deploy --env production"
  },
  "devDependencies": {
    "wrangler": "4.128.0"
  }
}
