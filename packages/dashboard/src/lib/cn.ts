// The class merger the design system ships, re-exported at the alias components.json names, so a
// registry copy-in resolves `@/lib/cn` without editing what it wrote.
export { cn } from '@vegastack/design'
