{
  "schemaVersion": 4,
  "foundationVersion": "0.4.0",
  "project": {
    "name": "compliant-web",
    "kind": "saas",
    "tier": "production",
    "tenancy": "multi-tenant-shared-schema"
  },
  "hosting": "cloudflare-opennext",
  "capabilities": ["web"],
  "notes": [
    "Better Auth is the identity library; sessions revalidate against durable storage",
    "RLS enabled and forced on every tenant table",
    "OpenBao runs as the production secrets service (multi-service identity trigger met)"
  ]
}
