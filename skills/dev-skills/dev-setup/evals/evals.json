{
  "skill_name": "dev-setup",
  "evals": [
    {
      "id": 1,
      "prompt": "Set up the dev workflow in this repo. It is vegastack/billing, a Bun monorepo (Hono API under `server/`, Next app under `app/`, Drizzle under `db/`), GitHub Actions in `.github/workflows/ci.yml`, changesets already configured, no UI tests. `AGENTS.md` exists with a `## Conventions` section the team wrote. My GitHub username is kmanojkumar. Use the defaults where they fit.",
      "expected_output": "A detection summary confirmed in one round, then `.vegastack/dev.md` rendered with the knob lines and the Ship/Verify sections drafted from the Bun playbook, the workflow labels created, and the AGENTS.md block inserted between the `vsk-dev` markers with the team's own section untouched.",
      "files": [],
      "assertions": [
        "`.vegastack/dev.md` carries the knob lines `review`, `gates`, `tests`, `branch`, `labels`, `changelog` with values",
        "The AGENTS.md insertion is delimited by `<!-- vsk-dev:start -->` and `<!-- vsk-dev:end -->`",
        "The text outside the markers (the team's `## Conventions` section) is unchanged",
        "The `architect:` knob is set to `kmanojkumar`"
      ]
    },
    {
      "id": 2,
      "prompt": "bootstrap this for agents — it's an empty folder called `acme-portal`, nothing in it yet, not even git. we'll be doing bun + hono, deploying to fly",
      "expected_output": "The greenfield path: the stack interview fills what an empty directory cannot, `git init` and `gh repo create` are each offered as their own yes/no question, and dev.md is rendered from the Bun playbook with TODO lines where the machinery (CI, changelog, release) does not exist yet.",
      "files": [],
      "assertions": [
        "`git init` and `gh repo create` are each offered as a separate question, neither run without a yes",
        "dev.md is rendered from a stack playbook, with TODO placeholder lines for absent machinery (CI, release, changelog)",
        "The empty directory is treated as a greenfield run, not as an error"
      ]
    },
    {
      "id": 3,
      "prompt": "Re-run dev-setup here. `.vegastack/dev.md` already exists from March with `review: subagent` and `gates: 2`; since then we added Playwright tests under `e2e/` and a release workflow. Update what changed.",
      "expected_output": "A re-run that detects the new machinery, proposes the affected lines (ui-evidence, the Ship runbook) and leaves the operator's existing knob values in place unless they say otherwise; the report lists exactly what changed.",
      "files": [],
      "assertions": [
        "No existing knob value (`review: subagent`, `gates: 2`) is overwritten without the operator's yes",
        "The newly detected machinery (Playwright, the release workflow) is named with the dev.md lines it affects",
        "The final report lists what changed and what was left alone"
      ]
    }
  ]
}
