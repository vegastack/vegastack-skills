# Refresh contract — vegafactory-setup

Evergreen: this skill asserts no version pins, vendor mechanism names, numeric vendor limits, or dated facts. The model and effort values in the seeded harness policy are the org's own defaults, edited in the control room rather than refreshed here. Revisit if a future edit introduces a volatile fact.
